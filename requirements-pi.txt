# Slim dependency set for a headless host (Raspberry Pi / VPS) running the
# Jarvis brain + remote bridges (Telegram, phone, remote tunnel, MCP). Excludes
# the heavy/desktop-only bits: torch, coqui-tts, chatterbox, opencv,
# sherpa-onnx (voice-id), pygrabber, pystray.
# TTS here is Piper (light, offline, ARM-friendly); STT is faster-whisper.
# System packages needed too - see PI_DEPLOY.md (libportaudio2, python3-tk).

anthropic>=0.40.0
faster-whisper>=1.0.0
openwakeword>=0.6.0
elevenlabs>=2.0.0
edge-tts>=6.1.0
piper-tts>=1.4.0
pygame>=2.5.0
sounddevice>=0.4.6
av>=11.0.0
Pillow>=10.0.0
numpy>=1.26,<2.2
scipy>=1.11.0
psutil>=5.9.0
requests>=2.31.0
beautifulsoup4>=4.12.0
ddgs>=9.0.0
pypdf>=5.0.0
python-docx>=1.1.0
fastembed>=0.3.0
segno>=1.6.0
# MCP client (optional)
mcp>=1.0.0
# Google Calendar/Gmail (optional - include if you want those tools)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
